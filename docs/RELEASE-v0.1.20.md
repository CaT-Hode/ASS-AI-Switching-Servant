# ASS v0.1.20

项目全称统一为 **Agent-Switching-Servant**，简称、窗口名与可执行文件仍为 **ASS**。

- 更新“关于 ASS”、授权回调页、README、许可证项目名称及 Windows 文件说明 / 版权信息。
- npm 项目标识改为 `agent-switching-servant`，依赖版本不变。
- 保留原数据目录、应用身份、配置迁移兼容规则、旧发布包识别和 GitHub 仓库地址。已有账户、OAuth 历史、模型和偏好无需重新导入。

这是命名更新，不改变路由、账户切换或客户端接入逻辑。等待当前任务结束，从托盘退出旧版 ASS，再启动完整解压的新包。
