# 验证范围 · ASS v0.1.19

## v0.1.19 新增检查

- 262 项 Node 核心测试通过，生产构建通过，生产依赖 audit 为 0 项已知漏洞。账户 / 模型、原生配置、代理配置与 OAuth 自动历史的隔离 Electron 回归通过；pi 能力稍后确认时，未变化的凭据也会补记。
- OAuth 自动记录、同身份刷新去重、同工作区不同用户隔离、opaque 授权保守去重；API 账户 / 不支持的服务拒绝；文件 / 环境 / 工作区限制；损坏文件、加密失败与不可解密历史均不覆盖。
- 切换预览只读、取消 / 过期票据不写入、票据单次使用、原生文件或已保存授权变化后拒绝提交；Codex 无关字段、Claude 设置与其他 API Key、pi 其他 provider 保留。写前事务重启恢复，外部再登录后不盲目回滚。旧 IPC 回包不能覆盖新状态。
- 隔离 Electron + Windows DPAPI 实测后台换号自动捕获、卡片去重、取消、确认切换、ASS 重启后保留历史并再次切换；前端未暴露合成令牌，加密文件未含明文身份 / 授权。零 renderer 页面错误。
- Windows x64 EXE 通过空白配置启动、OAuth 历史 / 切换、三类原生与两类代理接入、1100 / 1440 / 1920 横版新布局复测。源码账户 / 模型及可靠性回归通过；包内 70 个运行时文件字节一致，无用户配置、加密历史、设计图或测试目录。
- 可选重启：隔离 Electron 使用合成进程适配器验证开启 / 关闭时显式勾选、默认不重启、取消不改配置或结束进程、选择不跨弹窗保留。核心测试覆盖 PID 复用、安装变化、新子进程、ASS 自身保护、部分关闭 / 重启失败和活跃请求拒绝；失败不会冒充成功。首次并行 UI 运行有一次等待超时，随后源码及打包版复跑通过。
- Windows 进程关闭代码另行实测：仅创建本次 QA 的 Node / 隐藏控制台进程，确认关闭 4 个测试进程，保留另一测试进程树；失效身份及遗漏子进程被拒绝。真实 Codex 安装入口与进程清单只读识别通过，未执行真实 Codex / OpenCode 结束与重新启动；不将合成启动验证当成生产桌面重启已实测。
- 不用真实账户切换，没有重启生产客户端，没有付费调用。文件写入成功不是运行中客户端完成换号的证据，也不是服务端授权在线有效性的证明。Codex `auto / keyring / ephemeral` 不支持文件切换；pi 按安装版本确认 OAuth 服务。


## v0.1.18 新增检查

- 229 项核心测试通过：按供应商迁移 / 备份、后续新增模型继承开关、客户端范围独立、全部关闭后确认撤回、写入失败回滚、旧字段拒绝、失效安装路径与识别过滤。模型默认值交回原生客户端，账户选择不影响接入范围。
- Browser plugin not available，使用已有 Playwright 驱动隔离 Electron；1100 / 1440 / 1920 横版桌面截图检查，无水平溢出。修复窄桌面客户端名称断词；长供应商列表可独立滚动，客户端列表保持可达。原设计概念中的装饰说明和重复设置入口未采用。
- 新布局交互：供应商整组开关、只读展开、官方账户添加白名单、键盘操作无过渡、减少动态效果、确认取消、设置展开、客户端选择与接入偏好跨重启保留。身份正确、非空页面、无框架错误层、零页面 / 应用控制台错误。
- 修复账户卡片遗漏 `account.quota` 的显示与刷新；使用合成 OpenAI 5h / 周窗口验证分别显示剩余 80% / 55%。这些数值仅为测试数据，不是用户真实用量；缓存来源时间与错误分开保留。
- 账户 / 模型、原生接入、代理接入的隔离 Electron 回归均通过；生产依赖 audit 为 0 项已知漏洞。本轮没有重启生产 ASS 或 harness、修改真实凭据、发送付费模型请求。新客户端识别 / 协议适配不包含在本次通过范围。
- Windows x64 发布 EXE 已通过空白配置启动、新版布局 / 偏好重启、三类原生接入与两类代理接入回归；包内 68 个核心、Electron、构建资源逐字节匹配。更新了旧 QA 脚本依赖的按钮定位，可靠性回归再次通过。发布包未含测试账户、用户配置、设计图或测试目录，源码令牌形状检查通过。

## v0.1.17 新增检查

- 223 项核心测试通过：官方账户归属、后端绕过拒绝、零模型 API 启动、账户 / 默认模型解耦、全局模型范围、迁移备份、损坏配置拒绝、草稿与已应用快照、预览过期与活跃请求保护、原生 / 代理恢复及外部冲突。
- 真实隔离 Electron 回归：五类客户端账户卡片、添加服务白名单、原生配置同步 / 冲突 / 重启 / 撤回、模型列表全部行内操作、账户移除 / 重加 / 下次启动选择；无应用控制台错误。重复 React key 造成的接入面板残留已修复并加入断言。
- 代理 Electron 回归：Codex / Claude 真实 IPC 开关、范围 / 默认模型、草稿跨重启、显式同步、独立协议范围、官方 OAuth 与第三方 API 授权头分离、本地令牌禁止请求订阅、最后断开停止服务及原生 auth 字节不变。DPAPI 已应用记录不含明文凭据。
- 原生读取器：pi 0.73.1 ModelRegistry / AuthStorage / CLI 模型列表（含兼容 Codex OAuth 导入），OpenCode 1.18.31 `debug config/paths`，DSH 0.1.5-rc.1 Settings / Credentials 服务与启动覆盖配置组合接受生成结果。发现并修复 pi 必填 `apiKey` 声明、DSH 已移除 `tui` profile 两个运行时问题。
- 测试数据和截图位于仓库外的 ASS-validation，全部使用隔离目录和合成凭据。未登录真实账户、未调用付费模型、未重启生产 ASS / harness，未修改 DSH 源码。原生读取通过不等于上游权限或完整 Agent 任务通过。
- v0.1.17 Windows x64 打包 EXE 已完成空白配置启动、账户 / 模型、原生接入、代理接入回归；源码窗口可靠性回归继续通过。生产依赖 audit 为 0 项已知漏洞，源码 / ASAR 私密文件与令牌形状扫描通过；发布前重复核对核心源码与包内文件一致。

## v0.1.16 新增检查

- 203 项核心测试通过：小时保留、4 小时边界、周一跨年、闰月、当前未结束区间、未来时段排除、日账本不虚构小时、分时 / 日账本 / 总量对账、旧缓存重建、时区缓存失效及重启恢复。
- Browser plugin not available；使用已有 Playwright 驱动隔离 Electron，验证本周每格 / 柱 1 小时、本月每格 / 柱 4 小时、年视图仍为 365 日格 / 柱；用实际元素坐标断言先向下、再向右排列，日期 / 时间悬停与键盘明细正确。
- 1440 / 1100 横版桌面截图检查通过，未来格与无记录区分、DSH 日账本独立汇总且不分配到小时、客户端筛选及恢复汇总、加密缓存和视图选择跨重启保存；身份 / 非空 / 错误层 / 控制台检查通过。
- 使用合成数据验证界面与边界；账户 / 模型和原生接入回归通过，生产依赖 audit 为 0 项已知漏洞。本轮不修改生产账户或原生配置、不重启正在运行的 ASS / 客户端、不调用付费模型。

## v0.1.15 新增检查

- 196 项核心测试通过：新增累计 / 流式去重、OpenCode 推理与分叉、DSH 分组非重复加总、只读来源、增量缓存、日期筛选 / 分组对账、账户额度不叠加及筛选持久化；缺失计数不作为实测零。
- Browser plugin not available；使用已有 Playwright 驱动真实隔离 Electron 窗口，检查身份、非空首屏、无错误层、零应用控制台错误、365 天热力图、键盘日期明细、客户端筛选 / 返回汇总、月份与模型分类、Token 图、空数据、额度 5h / 周两窗口和重启后加密缓存恢复。
- 横版桌面 1440 / 1100 尺寸检查无横向溢出，截图人工检查可读。界面测试使用合成数据，截图与临时账户留在仓库外；不作为用户真实用量证据。
- 本机只读采集 Codex 历史、OpenCode 数据库和 DSH 账本，未打印提示词 / 工具内容、未保存真实账户到源码，未更改原生配置或重启客户端。Codex 部分超大记录明确标为部分读取；本机 Claude / pi 无历史记录，格式以合成样本验证。
- 原生接入与账户 / 模型回归通过；本次 DSH 检查确认“零绑定可用模型”是没有写出第三方目录的原因，不把开关开启当作模型已注入。

## v0.1.14 新增检查

- 185 项核心测试：新增诊断持久化 / 失效 / 取消 / 写入失败、排序与展示账户保存、品牌匹配、OpenAI / Claude 额度解析、五类原生 OAuth 读取、跨账户隔离与限频测试；原生配置协议分组、字段合并、OAuth 刷新保留、重启 / 撤回、事务回滚、并发改动拒绝、路径 / 格式 / 插值防护。
- 原生接入真实 Electron UI：DSH / OpenCode / pi 确认开启、编辑自动同步、原生 OAuth 保留、冲突可见并重试、DPAPI 恢复记录、退出保留 / 重启无代理、关闭开关撤回字段；Codex 原生凭据不变，零页面异常。生产客户端未重启，真实配置未改动。
- 本机 DSH 实际配置解析器与原生凭据适配器接受生成字段；pi-ai 0.85.1 对本机模拟上游发送 Chat Completions / Responses / Anthropic 请求，三种流均完整结束、授权头正确，没有经过 ASS 代理，也没有付费调用。OpenCode / pi 完整应用任务不在本次端到端实测范围。
- 真实 Electron 源码窗口：指针拖拽、Esc 取消、写入失败保留原序、筛选后方向键排序、完整退出重启恢复；1100 / 1380 / 1536 横版桌面无横向溢出，本地 logo 加载成功。
- 模型成功 / 失败实际经隔离路由和合成 SSE 测试，退出全部测试进程重开后状态及精确时间一致；磁盘为 DPAPI 加密。渲染器时间前移 2 小时后相对时间更新，没有额外模型调用。
- 额度合成案例：周窗口位于 primary、双窗口、显示账户与启动账户独立、401 保留原值及原时间、加密缓存和账户选择跨重启、缓存有效期内无重复查询。账户 / 模型与可靠性原有 UI 回归通过，零应用控制台错误。
- v0.1.14 打包 EXE 重复完成上述卡片 / 额度 / 持久化检查及原有账户、模型、可靠性回归，零应用控制台错误；生产依赖 audit 为 0 项已知漏洞。源码及发布包通过私密文件名 / 令牌形状扫描和用户导出配置的内存比对。
- 2026-09-22，通过与 ASS 相同的系统代理 / 系统 CA 请求实现，真实 GET OpenAI `/backend-api/wham/usage` 返回 HTTP 200。请求账户、响应账户与 Codex 工具当前账户一致；实际窗口时长、已用 / 剩余比例及重置时间与 Codex 工具独立读数逐项一致。真实账户 ID 和具体用量仅用于本机核对，不纳入公开记录。
- 实网检查未发送模型请求、未刷新令牌、未消费重置券；原生授权文件逐字节未变。真实额度原始数据及测试配置只在仓库外，不进入源码或发布包。其他供应商额度使用合成响应验证，未声称全部实网通过。

以下为历史版本验证记录。

## v0.1.13

2026-09-22，Windows / Node.js 24 / Electron 44；Browser plugin not available，使用已有 Playwright 驱动真实隔离 Electron 窗口。没有修改真实模型、账户或路由配置。

## v0.1.13 新增检查

- 148 项核心测试、生产构建、账户 / 模型和可靠性 UI 回归通过；生产依赖 audit 为 0 项已知漏洞。
- 单模型闪电仍只请求被选择模型；未测试为 SVG 空心，成功 / 失败均为实心且分别保持绿色 / 红色，悬停不覆盖结果颜色。模型列表和连接诊断页面一致。
- 测试中仅显示转圈；取消首次进行中的测试不产生结果，取消重新测试保留上次结果，跳过与排队取消不会被误标为已测试。关闭 / 重开列表及刷新渲染界面后保留状态。
- 页面身份、非空内容、无框架错误层、控制台零应用错误及真实截图检查通过。上游为合成响应，没有调用真实供应商模型或产生测试费用，也没有重启生产 ASS / harness。
- 本轮只改结果图标，不改变诊断持久化范围；应用进程退出后，原有的内存诊断结果仍不会跨重启恢复。

## v0.1.12 新增检查

- 148 项核心测试通过。覆盖 OpenCode 桌面版不在 PATH、ASAR 在 Node / Electron 中的文件 / 虚拟目录差异、CLI 与桌面端共存、随附 CLI、过期路径、明确选择与重启恢复；桌面版不会进入 CLI 授权启动流程。
- 实机只读检查确认 OpenCode Desktop 可被自动发现。真实 Electron 中显示“已安装桌面端”，打开图标的 IPC 使用 shell 替身核对目标文件；未启动真实 OpenCode，也没有切换或改写其账户。
- 五类客户端的接入开关仅出现在右侧详情状态行，左侧没有开关；切换客户端不会混用开关状态。取消不改变配置，确定后的接入状态独立并跨隔离应用重启保留，关闭仍走原有确认与恢复机制。
- 1100 / 1380 / 1536 横版窗口核验开关右对齐、无横向溢出，页面身份、非空内容、无框架错误层、控制台与截图检查通过。原有账户 / 模型和可靠性回归继续验证。
- 本轮未重新测试真实模型请求与所有供应商授权。仅桌面版的 OpenCode 独立账户 / 路由启动仍需要 CLI，不将“桌面已安装”报告为“CLI 接入可用”。

## v0.1.11 新增检查

- 142 项核心测试通过。新增相同模型字段顺序变化的保存 / 删除 / 重启验证，以及真实内容变化时拒绝过期删除的回归；没有移除并发修改保护。
- 模型行仅保留三个图标按钮；统一详情中展开配置或能力检测，往返不丢草稿，检测使用已保存模型。源码 UI、能力检测和原有账户 / 模型回归通过。
- 删除确认在按钮附近渲染，不调用系统消息框；取消、Esc 和窗外点击只退出当前确认，不触发删除或关闭父列表。模拟写入失败时确认窗显示原因、文件回滚且模型保留；同窗重试成功并在隔离应用重启后保持删除。
- 1100 / 1380 / 1536 桌面宽度实测窗外、面板间隙和父面板点击只返回一级；从输入框拖到窗外不关闭。嵌套确认的焦点与 Esc、其他管理弹窗以及减少动态效果通过。
- 页面身份、非空内容、无 Vite 错误层、控制台无应用错误及真实截图检查通过。上游响应、账户与故障使用合成夹具，不将其当作本次实网 API 或 OAuth 验证。

以下为历史版本验证记录。

2026-09-21，在 Windows / Node.js 24 / Electron 44 环境检查。测试数据目录与正常应用目录分离，未修改真实 Codex 全局配置。

## v0.1.9 新增检查

- 130 项单元测试、生产构建通过；生产依赖 audit 为 0 项已知漏洞。
- 隔离 Electron 窗口验证三级分离：一级不挂载模型表单 / 目录；点击卡片打开独立的二级原生对话框；三级对话框在右侧打开，二级向左收窄让位。主卡片页不能抢走焦点，三级打开时二级也不能获得编辑焦点。
- 返回保留搜索、筛选、滚动、焦点及行内草稿；模型详细弹窗与模型目录弹窗关闭后恢复原按钮焦点和二级宽度，快速连续打开 / 关闭、Escape 逐层关闭均通过。只读来源不出现编辑 / 添加按钮；减少动态效果模式取消位移。
- 行内五字段保存 / 取消、重复 ID、过时草稿和非法上下文拦截、删除确认 / 取消、重命名同步启动模型、发现 / 手动添加、闪电仅请求当前模型及重启持久化通过。原生测试凭据逐字节未变，快照无测试秘密值。
- 1100 / 1380 / 1536 宽桌面检查无横向溢出；核验两层弹窗顶边对齐、至少 12px 间隔、边界不越出窗口，二级确实向左移动且缩窄，关闭三级后宽度复原，并逐张检查真实截图。
- 原有高级模型编辑 / 思维滑块、余额设置、接入恢复、19 服务账户预设、合成 PKCE 与模型能力检测回归通过。网络响应为合成夹具，不代表本次重新验证真实官方 / 第三方连接。
- 使用项目 Playwright 驱动真实 Electron 独立窗口；不使用无法提供 preload 的普通网页预览。测试目录与截图在仓库外 `ASS-validation`，没有更改真实账户 / 注入，也没有重启生产客户端。

以下为历史版本验证记录。

## v0.1.8 新增检查

- 130 项单元测试通过；新增 14 项资料解析、工作区冲突、Claude 缓存隔离、白名单脱敏、精确官方 origin、缺失 / 零额度、币种、加密持久化、Key 轮换、并发合并、旧响应丢弃与网络失败测试。生产依赖 audit：0 项已知漏洞。
- 真实 Electron 开发版及打包 EXE：五类客户端资料、OpenRouter / DeepSeek 只读 GET、无自动账户 HTTP 请求、失败保留旧数据、重启恢复加密缓存、Key 更换立即隐藏旧资料、未绑定账户 IPC 拒绝均通过。原生测试凭据逐字节未变，快照不含测试 token / Key，页面 / 控制台无错误。
- 主侧栏底部服务卡片能显示就绪 / 停止；管理入口正常。鼠标悬停 / 按下、快速切换、键盘即时响应、减少动态效果无位移 / 缩放通过。1100、1380、1536 宽横版桌面无横向溢出，状态卡片在左栏底部可见。
- 原有账户绑定 / 解绑、行内模型编辑 / 删除 / 取消、重复与过时草稿拦截、模型目录自动读取、逐模型检查及重启持久化，在开发版和 v0.1.8 EXE 中回归通过。全新数据目录的打包窗口启动、五个接入开关、确认取消和焦点恢复通过。
- 实机只读确认本机 Codex 记录可提取 provider / email / plan / accountId / userId，未输出真实身份值或凭据。Claude 原生 `auth status` 的本机结果为未登录，不宣称其真实登录流程已经验证。
- 账户 API 查询使用合成响应，不宣称所有供应商真实接口 / 订阅 / 配额均已实测。Codex / Claude 身份信息为本地登录记录，不是实时权益核验。没有为本次验证请求新的 OAuth、刷新真实 token、启动模型请求或重启生产客户端。
- 当前工具不能通过普通网页复现 Electron preload，使用项目已有 Playwright 驱动真实独立窗口。新界面验证脚本、截图和私有测试资料在仓库外 `ASS-validation` 中，未随 Release 分发。

以下为历史版本验证，不能作为本次重新进行全量实网测试的证据。

## v0.1.5 新增检查

- 102 项测试通过；新增五类客户端本机凭据枚举、OAuth / API 分离、多供应商卡片稳定 ID、过期 / 可刷新 / 损坏 / 密钥库状态、环境与自定义目录、秘密值不出现在快照、原生文件不改写、账户 / 模型 / UI 偏好重启恢复，以及模型目录去重 / 缓存 / 取消 / 失效检查。
- 独立 Electron 实际窗口：五枚接入胶囊、简短确认与取消、全部 harness 的 OAuth 测试卡片、供应商 API 模型选择、模型目录自动获取与一键添加通过。实际关闭并重启测试应用后，账户、模型、添加的模型、当前客户端 / 页面和接入开关保留。
- 刷新后删除的测试 OAuth 不再出现。原生凭据测试文件逐字节核对无变化，前端快照不包含测试令牌。1380 / 1100 横版窗口均为右侧双列卡片，无横向溢出；无页面或控制台错误。移除重复 OAuth 状态视图与说明性口号。
- 原有模型编辑、余额设置、接入 / 恢复、加密保存、19 官方服务、合成 PKCE 回调与 DSH 源码目录识别回归通过。
- 真实本机只读检查：Codex OAuth 可被识别；OpenCode / DSH 发现 API 凭据，Claude / Pi 默认位置暂无凭据。没有联网验证其授权，没有刷新真实令牌、修改真实账户或中断生产 ASS / harness。
- Browser plugin not available，使用已有 Playwright 驱动隔离 Electron；截图和扩展测试脚本在仓库外。本地凭据存在不等于远端授权有效；系统密钥库、未知类型与外部动态密钥不冒充已验证。DSH OAuth 依据本机源码的 credentials-local / llm-pi-ai 记录格式实现。
- 打包后的 v0.1.5 EXE 重新运行空数据启动与完整五客户端卡片 / 重启检查，全部通过；并非仅验证开发环境。npm audit 为 0 已知漏洞。

## v0.1.4 新增检查

- 87 项核心测试通过；生产构建通过。没有更改后台接入、进程身份、请求计数或配置恢复逻辑。
- 实际 Electron 窗口：开启、断开、停止全部统一为一段提示和两枚按钮，无分步菜单、复选框、单选项、PID 清单或额外关闭按钮。取消按钮、Escape、默认回车均不修改配置，关闭后恢复开关焦点。
- 确定一次即可执行；模拟 ASS 窗口验证只关闭选定客户端，保留其他客户端接入、共享服务和 auth.json。有活动请求或确认期间状态变化时拒绝操作；错误仍只使用原有提示区及两枚按钮。
- 最后断开恢复测试 Codex 配置并停止共享服务，全关闭状态跨测试版重启保留。1100 / 1380 桌面布局、减少动态效果及页面 / 控制台零错误通过；模型编辑、余额设置、接入 / 恢复和加密落盘回归通过。
- Browser plugin 不可用，使用项目现有 Playwright 驱动独立 Electron 窗口；截图及扩展检查脚本位于仓库外。所有测试使用隔离数据目录，本次没有启动或关闭真实 harness，也没有重启生产 ASS。

下列条目为历史版本验证；不表示本版重新实测所有第三方模型、OAuth 或官方订阅请求。

## v0.1.3 新增检查

- 87 项核心测试通过。新增接入生命周期、确认票据与过期 / 重用拒绝、客户端隔离、上传阶段请求计数、配置冲突、恢复记录、进程身份与终止失败等回归。
- Electron 实际窗口：全局 Codex 接入按钮已不存在；五种客户端各自的接入面板、两层确认、默认安全断开、勾选确认、取消 / Escape 不改配置、请求进行中拒绝关闭、过期状态重新预览通过。
- 合成窗口清单验证：只结束选中 DSH 窗口，其他 Claude 窗口与 pi 接入不受影响；清理 DSH 注入文件但保留 auth.json；最后关闭全部时恢复 Codex 配置并停止服务。进程终止在这一 UI 检查中使用合成适配器，不是假称关闭了真实 harness。
- Windows 原生进程管理另做实测：只创建本次 QA 专用的隐藏休眠根进程与 3 个子进程，经随机标记 / PID / 创建时间核对后终止并验证退出，没有启动或关闭用户真实 harness。未验证主动脱离进程树的后台服务，也不将进程树检查视为 Job Object 级别的隔离。
- 全部关闭状态在实际退出并重启测试版后仍保留；1100 / 1380 桌面布局、减少动态效果、页面身份、非空页面及控制台零错误通过。Browser plugin not available，使用项目已有 Playwright 驱动独立 Electron 窗口，截图和临时测试脚本保存在仓库外。
- 复核修正首次启动全关闭仍监听的问题；恢复文件时逐个检查并发外部修改，发生冲突保留剩余文件和恢复记录。模型编辑、余额设置、19 服务账户页、PKCE 合成回调与 DSH 源码识别原有 UI 回归通过。
- npm audit 在本版发布准备阶段为 0 项已知漏洞。没有修改实际 Codex 全局配置、真实账户、生产路由或旧版快捷方式。

下列条目为历史版本的验证证据；没有在本版重新实测所有第三方模型、OAuth 或官方订阅请求。

## v0.1.2 新增检查

- 55 项核心测试通过：新增语义版本比较、禁止降级、稳定 / 预览渠道、官方仓库链接与资源上传状态验证、新旧 ZIP 文件名兼容、ETag 与持久化缓存、定时检查、关闭自动后手动检查、并发合并、取消、断网、限流和过大响应拒绝。
- 真实 Electron 窗口：启动定时器自动发现模拟新版（未点击手动检查）、下载按钮对应正确发布资源、预览筛选、自动检查开关保存、关闭自动后手动检查、收起提示、错误恢复及减少动态效果均通过。
- 实网读取本仓库 GitHub Releases，经 Electron 系统网络栈返回有效结果；在隔离测试中将当前版本设为 0.1.0，成功识别当时发布的 v0.1.1 及旧名称 ZIP。未携带 GitHub 或模型凭据，未下载或执行更新包。
- 1380 / 1100 宽桌面检查，无横向溢出、空白页、框架错误或页面 / 控制台异常。Browser plugin not available，使用项目已有 Playwright 驱动真实 Electron。测试截图在仓库外，不随发布包分发。
- 品牌主显示改为 ASS；全称只保留为辅助说明和历史数据目录。原有账户、模型和配置接入 / 恢复交互回归通过。没有改名用户数据目录，也没有重启正在运行的旧版本。

以下为此前版本的验证记录，只有以上列出的项目及本次自动回归是新证据，不代表本次重新实测全部供应商与 OAuth。

## v0.1.1 新增检查

- 44 项核心测试通过：保留此前路由与隔离测试，增加启动目录解析、模型级标识、元数据白名单、非法思维档位对照、取消与限流、19 组官方服务归类、Gemini 版本路径、原生 Key 加密、PKCE 回调 / 取消 / 超时 / 密钥不回传。
- 实际 Electron 窗口：逐模型闪电只发送被选模型；模型目录读取和添加、能力报告、两套 OpenAI API 账户、独立 ChatGPT 授权目录、Cursor Key 保管、OpenRouter 合成授权回调均通过。没有代替用户完成官网授权。
- 自动识别与选择 `D:\deepseek-harness` 实际源码目录，解析到 `apps/cli/lib/bin.js`，Node 运行该入口的 `--version` 返回 `0.1.5-rc.1`。未修改 DSH 源码、安装依赖或改动用户工作目录。
- 新账户页面在 1380 / 1100 宽桌面窗口检查，无横向页面溢出、无页面或控制台错误。截图保存在发布仓库外。Browser plugin 不可用，使用 Playwright 控制真实 Electron 窗口，不用静态页面代替客户端验收。
- 单个 MiMo Pro 模型实网探测 4 条请求：Responses 完整结束；工具结构与随机标记匹配；low 参数接受，非法 effort 被 HTTP 400 参数校验拒绝。读取 `/models` 得到 276 个目录项。未实测其余思维档位、276 个模型、长上下文或视觉能力；目录条数不等于能力验证数。
- 官方账户页使用 Apple 风格的既有桌面视觉体系；只复用弹窗入退场和减少动态效果设置，不给频繁账户选择增加动效。

以下原生客户端 / 实网记录保留自 v0.1.0，除上述明确列出的新检查外，不宣称在 v0.1.1 对所有上游重复验证。

## 自动检查

- 核心测试：导入、目录字段、配置接入/恢复、凭据分流、同源余额请求、SSE 截断与无 Content-Type 检查。
- 五种 harness 配置生成、官方与 API 环境隔离、账户切换、OAuth 格式转换、来源文件不变、pi 新账户导入、不向渲染进程发送令牌。
- Electron 窗口 UI：模型编辑、非 GPT ultra 默认限制、余额设置、配置接入/恢复、DPAPI 落盘、账户创建、键盘 Escape 和焦点恢复、减少动态效果、1100px 横向窗口无横向溢出；无页面异常。
- npm audit：发布准备阶段 0 项已知漏洞。

## 原生客户端检查

- 本机 Codex 实际加载生成目录，官方模型、MiMo Responses、Anthropic 转换文本请求完成。
- 工具验证中，官方与 MiMo 的调用进入了 Codex 命令执行路径，但被本机只读策略拒绝；未绕过策略，因此不宣称命令执行成功。当前 Codex 某些模式只在提示内声明工具，不传标准 `tools` 数组：Anthropic 转换在这种模式下没有可转换的函数定义，尚不具备完整 coding-agent 工具兼容性。请优先使用原生 Responses，或从 ASS 启动 Claude Code / pi 的 Anthropic 接入。
- pi 0.73.1 实际读取生成的模型目录；运行时读取到 anthropic / github-copilot / openai-codex OAuth 类型。其原生 AuthStorage 读取 ASS 转换的合成 Codex OAuth 文件，确认识别为已配置授权账户；来源文件不变，未调用刷新接口。
- OpenCode 1.18.31 实际解析 inline provider 配置，模型为 `ass/test-chat`，XDG 数据目录隔离生效。
- DeepSeek Harness 0.1.5-rc.2 的实际 `llm-pi-ai` schema 接受生成配置，CLI 版本和默认启动配置可读取。
- Claude Code 2.1.223 原生 auth 子命令可用。API 环境及 Anthropic 协议转发通过自动检查。

## 实网边界

- 当前 Windows CA 环境中，官方 Codex HTTP/SSE 返回完整 `response.completed`。
- 两个实际第三方模型入口返回完整响应；另一个供应商检测出现不完整流，ASS 未将其标为成功。
- Sub2API 余额接口实测成功；另两个通用中转余额探测返回 HTTP 400。其余内置适配器使用响应样本单测，不宣称全部实网验证。
- 未替用户完成新的 OAuth 网页授权，未撤销或刷新用户来源账户。OAuth 导入以格式、隔离目录、原生注册表和不可覆盖测试为证据；供应商是否允许刷新或使用由原生客户端验证。
- 不将简单文本连接检测等同于全部工具、文件、图像、长上下文、跨客户端订阅权益验证。

## 发布包检查

- Windows x64 便携版以全新测试目录启动独立窗口；检查空供应商列表、五种客户端页面、pi OAuth 导入区域、Windows 凭据加密可用及本地服务启动。
- 包内排除私有测试目录、账户文件、配置导出、开发依赖和测试脚本；校验 SHA-256 随 ZIP 发布。

没有把真实 API Key、用户导出或测试账户打入源码和发布包。UI 截图与私有测试数据不随源码发布。
