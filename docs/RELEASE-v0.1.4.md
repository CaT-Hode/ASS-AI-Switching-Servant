# ASS v0.1.4 · 确认更简洁

- 接入开关、停止全部和安全退出统一为简短警告 + 确定 / 取消。
- 移除分步菜单、勾选确认、窗口明细和 PID 清单；默认焦点在取消。
- 断开前提示将关闭的 ASS 窗口数量，确定后执行。进行中请求、进程身份、状态变化和配置冲突检查全部保留；不自动重启客户端。

87 项核心测试、构建及独立 Electron 窗口交互检查通过。未修改真实账户或重启正在使用的 ASS。

## 升级

等待当前任务结束，从旧版托盘退出 ASS，完整解压 ZIP 后运行新版 ASS.exe。账户数据仍使用 `%APPDATA%\AI Switch Servant`，无需重新导入；原快捷方式需指向新版目录。这是未签名的 Windows x64 预览版，请核对 SHA256SUMS.txt。
